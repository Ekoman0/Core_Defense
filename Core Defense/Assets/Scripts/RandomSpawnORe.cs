using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomSpawnORe : MonoBehaviour
{
   /* public List<GameObject> SpawnPoints;
    private int randomSpawnPoints;
    private int randommiktarright;
    private int randommiktarleft;
    private int randommiktarup;
    private int randommiktardown;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        randomSpawnPoints = Random.Range(0, SpawnPoints.Count);
        randommiktarright = Random.Range(0, SpawnPoints.Count);

        for (int i = 0; i < randommiktarright; i++)
        {
            Instantiate()
        }
        SpawnPoints[randomSpawnPoints]
    }*/
}
